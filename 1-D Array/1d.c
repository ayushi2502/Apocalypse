#include<stdio.h>
void main()
{
  int i,n,c,a[i];
  printf("entr value");
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d", &a[i]);
   }
   for(i=0;i<n;i++);
   {
      if(c<0)
        {
          printf("%d",c);
           c++;
        }
        else
        {
          printf("%d",a[i]);
        }
    }
}
